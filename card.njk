{% extends "design-library-base.njk" %}
{% set activePage = 'card' %}

{% block componentContent %}
<div class="max-w-4xl mx-auto px-6 py-8">
    
    <!-- Page Title -->
    <h1 class="text-4xl font-bold text-gray-900 mb-8">Card</h1>

    <!-- Primary Example Block -->
    <div class="border border-gray-300 rounded-lg mb-8">

        <!-- Live Example -->
        <div class="p-10 bg-gray-50 relative min-h-96">
            <!-- Card Content (positioned on left side, behind image) -->
            <div class="sw-card-a relative w-80 bg-teal-400 text-white shadow-lg z-5" style="height: 280px; margin-top: 2rem;">
                <!-- Card Content with right padding to prevent overlap -->
                <div class="p-8 pr-32">
                    <h2 class="text-2xl font-bold mb-4 leading-tight">Grants & funding</h2>
                    <p class="text-base leading-relaxed">
                        We distribute funding to community clubs, volunteers, and athletes. Our grants team can support you with information.
                    </p>
                </div>
            </div>

            <!-- Angled Image Container (in front, slightly wider) -->
            <div class="sw-angled-image absolute top-0 right-4 w-64 h-96 z-20">
                <div class="w-full h-full relative">
                    <!-- Sport Wales SVG Image with angled clip-path -->
                    <img src="../img/card-layout-no-button-graphic.svg" 
                         alt="Sport Wales - Grants and funding" 
                         class="absolute inset-0 w-full h-full object-cover"
                         style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);" />
                    
                    <!-- Red overlay filter for Sport Wales branding -->
                    <div class="absolute inset-0 bg-red-500 opacity-60 mix-blend-multiply"
                         style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);"></div>
                </div>
            </div>
        </div>
        
        <!-- React Code Tabs -->
        <div class="border-t border-gray-200">
            <div id="primary-code-tabs"></div>
        </div>
        
    </div>

    <!-- When to use this component -->
    <section class="mb-12">
        <h2 class="text-2xl font-bold text-gray-900 mb-4">When to use this component</h2>
        <p class="text-gray-700 leading-relaxed mb-4">
            Use the card component to present information in a structured, visually appealing format. Cards work well for displaying related content like services, features, or key information that users need to scan quickly.
        </p>
        <p class="text-gray-700 leading-relaxed mb-4">
            Cards are particularly effective for landing pages, service overviews, and content that benefits from visual hierarchy and clear separation.
        </p>
    </section>

    <!-- How it works -->
    <section class="mb-12">
        <h2 class="text-2xl font-bold text-gray-900 mb-4">How it works</h2>
        <p class="text-gray-700 leading-relaxed mb-4">
            Cards should present information clearly and consistently. Follow these guidelines for effective card design:
        </p>
        
        <ul class="list-disc ml-6 space-y-2 text-gray-700 leading-relaxed mb-6">
            <li>Use consistent card dimensions for visual harmony (recommended width: 296px for content area)</li>
            <li>Include a clear, descriptive heading that summarizes the card content</li>
            <li>Keep description text concise but informative</li>
            <li>Use Sport Wales brand colors: teal (#14B8A6) for primary cards</li>
            <li>Include visual elements (graphics/icons) to enhance understanding</li>
            <li>Ensure adequate padding for comfortable reading (35-40px)</li>
            <li>Use white text on colored backgrounds for optimal contrast</li>
            <li>Consider responsive design for different screen sizes</li>
            <li>Group related cards together for better user experience</li>
            <li>Use consistent typography (Montserrat font family)</li>
            <li>Include visual cues like graphics to support the content</li>
            <li>Test card layouts across different devices and screen sizes</li>
        </ul>

        <h3 class="text-xl font-semibold text-gray-900 mb-3">Interactive Features</h3>
        <ul class="space-y-2 text-gray-700 leading-relaxed">
            <li class="flex items-start">
                <span class="text-green-500 mr-2">✓</span>
                <span>Visual graphics enhance content understanding</span>
            </li>
            <li class="flex items-start">
                <span class="text-green-500 mr-2">✓</span>
                <span>Consistent spacing and typography for professional appearance</span>
            </li>
            <li class="flex items-start">
                <span class="text-green-500 mr-2">✓</span>
                <span>Responsive design adapts to different screen sizes</span>
            </li>
            <li class="flex items-start">
                <span class="text-green-500 mr-2">✓</span>
                <span>Clear content hierarchy with heading and description</span>
            </li>
            <li class="flex items-start">
                <span class="text-green-500 mr-2">✓</span>
                <span>Sport Wales brand color implementation</span>
            </li>
            <li class="flex items-start">
                <span class="text-green-500 mr-2">✓</span>
                <span>Shadow effects for visual depth and separation</span>
            </li>
            <li class="flex items-start">
                <span class="text-green-500 mr-2">✓</span>
                <span>Scalable vector graphics for crisp display at any size</span>
            </li>
            <li class="flex items-start">
                <span class="text-green-500 mr-2">✓</span>
                <span>Accessible color contrast ratios</span>
            </li>
        </ul>
    </section>

    <!-- Card with Interactive Arrow -->
    <section class="mb-12">
        <h2 class="text-2xl font-bold text-gray-900 mb-4">Card with Interactive Arrow</h2>
        <p class="text-gray-700 leading-relaxed mb-6">
            Use this card variant when you want to indicate that the card is clickable or leads to more detailed information. The arrow provides a visual cue for user interaction.
        </p>

        <div class="border border-gray-300 rounded-lg mb-8">
            <div class="p-10 bg-gray-50 relative min-h-96">
                <!-- Card Content (positioned on left side, behind image, clickable) -->
                <div class="sw-card-b relative w-80 bg-teal-400 text-white shadow-lg z-5 cursor-pointer hover:bg-teal-500 transition-colors duration-300" style="height: 280px; margin-top: 2rem;" onclick="alert('Card clicked! This would navigate to grants & funding page.')">
                    <!-- Card Content with right padding to prevent overlap -->
                    <div class="p-8 pr-32">
                        <h2 class="text-2xl font-bold mb-4 leading-tight">Grants & funding</h2>
                        <p class="text-base leading-relaxed">
                            We distribute funding to community clubs, volunteers, and athletes. Our grants team can support you with information.
                        </p>
                    </div>
                </div>

                <!-- Angled Image Container (in front, slightly wider) -->
                <div class="sw-angled-image absolute top-0 right-4 w-64 h-96 z-20">
                    <div class="w-full h-full relative">
                        <!-- Sport Wales SVG Image with angled clip-path -->
                        <img src="../img/card-layout-no-button-graphic.svg" 
                             alt="Sport Wales - Grants and funding" 
                             class="absolute inset-0 w-full h-full object-cover"
                             style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);" />
                        
                        <!-- Red overlay filter for Sport Wales branding -->
                        <div class="absolute inset-0 bg-red-500 opacity-60 mix-blend-multiply"
                             style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);"></div>
                        
                        <!-- Interactive Arrow Overlay (positioned on the teal card area) -->
                        <div class="absolute top-1/2 left-6 transform -translate-y-1/2 z-30 pointer-events-none">
                            <div class="bg-white bg-opacity-20 rounded-full p-3 backdrop-blur-sm">
                                <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 24 24">
                                    <path d="M8.59 16.59 L13.17 12 L8.59 7.41 L10 6 L16 12 L10 18 L8.59 16.59Z" />
                                </svg>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="border-t border-gray-200">
                <div id="interactive-code-tabs"></div>
            </div>
        </div>
    </section>

    <!-- Sport Wales Guidelines -->
    <section class="mb-12 bg-blue-50 border border-blue-200 rounded-lg p-6">
        <h2 class="text-2xl font-bold text-blue-900 mb-4">Sport Wales Guidelines</h2>
        <ul class="list-disc ml-6 space-y-2 text-blue-800 leading-relaxed">
            <li>Cards should work best on content with a 1440px width for optimal desktop display</li>
            <li>Use teal (#14B8A6) as the primary background color for consistency with Sport Wales branding</li>
            <li>Maintain consistent padding of 35-40px for comfortable content spacing</li>
            <li>Include visual graphics to enhance content understanding and brand identity</li>
            <li>Ensure white text on colored backgrounds meets accessibility contrast requirements</li>
            <li>Use Montserrat font family for all card text to maintain brand consistency</li>
            <li>Consider hover states for interactive cards to provide clear user feedback</li>
            <li>Test card layouts across different devices to ensure responsive behavior</li>
            <li>Group related cards together for improved user experience and content organization</li>
            <li>Use consistent card dimensions within the same layout for visual harmony</li>
        </ul>
    </section>

</div>

<!-- React CDN -->
<script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
<script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
<script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

<!-- Highlight.js CDN - Dark Theme -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/styles/github-dark.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/highlight.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/languages/javascript.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/languages/xml.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/languages/css.min.js"></script>
<script>
// Initialize highlight.js when loaded
hljs.highlightAll();
</script>

<script type="text/babel">
{% raw %}
const { useState } = React;

// Reusable code tabs component  
function CodeTabsComponent({ codeExamples, tabId }) {
    const [codeType, setCodeType] = useState('html');

    // Language mapping for highlight.js
    const getLanguageClass = (type) => {
        const languageMap = {
            html: 'language-html',
            css: 'language-css', 
            react: 'language-jsx',
            tailwind: 'language-html' // HTML with Tailwind classes
        };
        return languageMap[type] || 'language-html';
    };

    // Re-highlight code when codeType changes
    React.useEffect(() => {
        // Wait for highlight.js to be loaded
        const highlightCode = () => {
            if (window.hljs) {
                // Small delay to ensure DOM is updated
                setTimeout(() => {
                    const codeBlock = document.querySelector(`#${tabId}-code-block`);
                    if (codeBlock) {
                        // Remove existing highlighting
                        codeBlock.removeAttribute('data-highlighted');
                        codeBlock.className = getLanguageClass(codeType);
                        // Re-highlight
                        window.hljs.highlightElement(codeBlock);
                    }
                }, 50);
            } else {
                // If hljs not loaded yet, try again in 100ms
                setTimeout(highlightCode, 100);
            }
        };
        
        highlightCode();
    }, [codeType, tabId]);

    return (
        <div>
            {/* Code Type Selector */}
            <div className="flex" role="tablist">
                {Object.keys(codeExamples).map((type, index) => (
                    <button
                        key={type}
                        onClick={() => setCodeType(type)}
                        className={`px-4 py-3 text-sm font-semibold border-b-3 transition-all ${
                            codeType === type
                                ? 'border-blue-600 bg-white text-blue-600'
                                : 'border-transparent bg-gray-100 text-gray-600 hover:bg-gray-200'
                        }`}
                    >
                        {type.toUpperCase()}
                    </button>
                ))}
            </div>

            {/* Code Display */}
            <div className="bg-white">
                <div className="relative">
                    <button
                        onClick={() => {
                            navigator.clipboard.writeText(codeExamples[codeType]);
                            // Visual feedback
                            const button = event.target;
                            const originalText = button.textContent;
                            button.textContent = 'Copied!';
                            setTimeout(() => {
                                button.textContent = originalText;
                            }, 2000);
                        }}
                        className="absolute top-2 right-2 px-3 py-1 text-xs bg-gray-700 text-gray-200 hover:bg-gray-600 rounded transition-colors duration-300 border border-gray-600"
                    >
                        Copy code
                    </button>
                    <pre className="p-4 bg-gray-900 text-gray-100 rounded text-sm font-mono overflow-x-auto border border-gray-700">
                        <code 
                            id={`${tabId}-code-block`}
                            className={getLanguageClass(codeType)}
                        >
                            {codeExamples[codeType]}
                        </code>
                    </pre>
                </div>
            </div>
        </div>
    );
}

// Main Component with all code examples
function ComponentInstructions() {
    const componentData = {
        codeExamples: {
            primary: {
                html: `<!-- Basic Card with Angled Image Container (Improved Design) -->
<div class="relative w-full min-h-96 p-10 bg-gray-50">
    <!-- Card Content (positioned on left side, behind image) -->
    <div class="sw-card-basic relative w-80 bg-teal-400 text-white shadow-lg z-5" style="height: 280px; margin-top: 2rem;">
        <!-- Card Content with right padding to prevent overlap -->
        <div class="p-8 pr-32">
            <h2 class="text-2xl font-bold mb-4 leading-tight">Grants & funding</h2>
            <p class="text-base leading-relaxed">
                We distribute funding to community clubs, volunteers, and athletes. Our grants team can support you with information.
            </p>
        </div>
    </div>

    <!-- Angled Image Container (in front, slightly wider) -->
    <div class="sw-angled-image absolute top-0 right-4 w-64 h-96 z-20">
        <div class="w-full h-full relative">
            <!-- Replace this background with your image -->
            <img src="your-sport-wales-image.jpg" alt="Grants and funding" 
                 class="absolute inset-0 w-full h-full object-cover"
                 style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);" />
            
            <!-- Red overlay filter for Sport Wales branding -->
            <div class="absolute inset-0 bg-red-500 opacity-60 mix-blend-multiply"
                 style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);"></div>
        </div>
    </div>
</div>`,
                css: `.sw-card-basic {
    position: relative;
    width: 320px; /* w-80 - Fixed width */
    height: 280px; /* Fixed height as specified */
    background-color: #14B8A6; /* teal-400 */
    color: white;
    /* No border-radius - removed for sharp corners */
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    font-family: 'Montserrat', sans-serif;
    z-index: 5; /* Behind the image */
}

.sw-card-basic .card-content {
    padding: 32px;
    padding-right: 128px; /* Right padding to prevent overlap with image */
}

.sw-card-basic .card-title {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 16px;
    line-height: 1.25;
}

.sw-card-basic .card-description {
    font-size: 16px;
    line-height: 1.625;
}

/* Angled Image Container */
.sw-angled-image {
    width: 256px; /* w-64 - Wider than before */
    height: 384px; /* h-96 */
    z-index: 20; /* In front of the card */
}

.sw-angled-image img {
    object-fit: cover;
    object-position: center;
    clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);
}`,
                react: `function BasicCard({ title, description, imageUrl, imageAlt }) {
    return (
        <div className="relative w-full min-h-96 p-10 bg-gray-50">
            {/* Card Content (positioned on left side, behind image) */}
            <div className="sw-card-basic relative w-80 bg-teal-400 text-white shadow-lg z-5" style={{height: '280px', marginTop: '2rem'}}>
                <div className="p-8 pr-32">
                    <h2 className="text-2xl font-bold mb-4 leading-tight">{title}</h2>
                    <p className="text-base leading-relaxed">
                        {description}
                    </p>
                </div>
            </div>

            {/* Angled Image Container (in front, slightly wider) */}
            <div className="sw-angled-image absolute top-0 right-4 w-64 h-96 z-20">
                <div className="w-full h-full relative">
                    {imageUrl ? (
                        <>
                            <img 
                                src={imageUrl} 
                                alt={imageAlt || "Card image"} 
                                className="absolute inset-0 w-full h-full object-cover"
                                style={{clipPath: 'polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%)'}}
                            />
                            {/* Red overlay filter */}
                            <div className="absolute inset-0 bg-red-500 opacity-60 mix-blend-multiply" 
                                 style={{clipPath: 'polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%)'}}></div>
                        </>
                    ) : (
                        <div className="absolute inset-0 flex items-center justify-center text-white bg-gradient-to-br from-red-400 to-red-600" 
                             style={{clipPath: 'polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%)'}}>
                            <div className="text-center p-6">
                                <div className="text-base font-bold">SPORT WALES</div>
                                <div className="text-sm opacity-90">Image Placeholder</div>
                            </div>
                        </div>
                    )}
                </div>
            </div>
        </div>
    );
}

// Usage Example
<BasicCard 
    title="Grants & funding"
    description="We distribute funding to community clubs, volunteers, and athletes. Our grants team can support you with information."
    imageUrl="your-sport-wales-image.jpg"
    imageAlt="Sport Wales - Grants and funding"
/>`,
                tailwind: `<!-- Basic Card with Angled Image Container (Updated Structure) -->
<div class="relative w-full min-h-96 p-10 bg-gray-50" style="font-family: 'Montserrat', sans-serif;">
    <!-- Card Content (positioned on left side, behind image) -->
    <div class="sw-card-basic relative w-80 bg-teal-400 text-white shadow-lg z-5" style="height: 280px; margin-top: 2rem;">
        <div class="p-8 pr-32">
            <h2 class="text-2xl font-bold mb-4 leading-tight">Grants & funding</h2>
            <p class="text-base leading-relaxed">
                We distribute funding to community clubs, volunteers, and athletes. Our grants team can support you with information.
            </p>
        </div>
    </div>

    <!-- Angled Image Container (in front, slightly wider) -->
    <div class="sw-angled-image absolute top-0 right-4 w-64 h-96 z-20">
        <div class="w-full h-full relative">
            <!-- Replace with your Sport Wales image -->
            <img src="your-sport-wales-image.jpg" 
                 alt="Sport Wales - Your content description" 
                 class="absolute inset-0 w-full h-full object-cover"
                 style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);" />
            
            <!-- Red overlay filter for Sport Wales branding -->
            <div class="absolute inset-0 bg-red-500 opacity-60 mix-blend-multiply"
                 style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);"></div>
        </div>
    </div>
</div>`
            },
            
interactive: {
    html: `<!-- Interactive Card with Angled Image and Arrow (Improved Design) -->
<div class="relative w-full min-h-96 p-10 bg-gray-50">
    <!-- Card Content (positioned on left side, behind image, clickable) -->
    <div class="sw-card-interactive relative w-80 bg-teal-400 hover:bg-teal-500 text-white shadow-lg cursor-pointer transition-colors duration-300 z-5" 
         style="height: 280px; margin-top: 2rem;" onclick="handleCardClick()">
        <!-- Card Content with right padding to prevent overlap -->
        <div class="p-8 pr-32">
            <h2 class="text-2xl font-bold mb-4 leading-tight">Grants & funding</h2>
            <p class="text-base leading-relaxed">
                We distribute funding to community clubs, volunteers, and athletes. Our grants team can support you with information.
            </p>
        </div>
    </div>

    <!-- Angled Image Container (in front, slightly wider) -->
    <div class="sw-angled-image absolute top-0 right-4 w-64 h-96 z-20">
        <div class="w-full h-full relative">
            <!-- Replace this background with your image -->
            <img src="your-sport-wales-image.jpg" alt="Grants and funding" 
                 class="absolute inset-0 w-full h-full object-cover"
                 style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);" />
            
            <!-- Red overlay filter for Sport Wales branding -->
            <div class="absolute inset-0 bg-red-500 opacity-60 mix-blend-multiply"
                 style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);"></div>
            
            <!-- Interactive Arrow (positioned on left side of angled area) -->
            <div class="absolute top-1/2 left-6 transform -translate-y-1/2 z-30">
                <div class="bg-white bg-opacity-20 rounded-full p-3 backdrop-blur-sm hover:bg-opacity-30 transition-all duration-300">
                    <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M8.59 16.59 L13.17 12 L8.59 7.41 L10 6 L16 12 L10 18 L8.59 16.59Z" />
                    </svg>
                </div>
            </div>
        </div>
    </div>
</div>

`,
    css: `.sw-card-interactive {
    position: relative;
    width: 320px;
    height: 280px;
    background-color: #14B8A6;
    color: white;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    cursor: pointer;
    transition: background-color 0.3s ease;
    font-family: 'Montserrat', sans-serif;
    z-index: 5;
}

.sw-card-interactive:hover {
    background-color: #0D9488;
}

.sw-card-interactive .card-content {
    padding: 32px;
    padding-right: 128px;
}

.sw-card-interactive .card-title {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 16px;
    line-height: 1.25;
}

.sw-card-interactive .card-description {
    font-size: 16px;
    line-height: 1.625;
}

.sw-angled-image {
    width: 256px;
    height: 384px;
    z-index: 20;
}

.sw-angled-image img {
    object-fit: cover;
    object-position: center;
    clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);
}`,
    react: `function InteractiveCard({ title, description, imageUrl, imageAlt, onClick }) {
    return (
        <div className="relative w-full min-h-96 p-10 bg-gray-50">
            <div 
                className="sw-card-interactive relative w-80 bg-teal-400 hover:bg-teal-500 text-white shadow-lg cursor-pointer transition-colors duration-300 z-5" 
                style={{'height': '280px', 'marginTop': '2rem'}}
                onClick={onClick}
                role="button"
                tabIndex={0}
                onKeyPress={(e) => {
                    if (e.key === 'Enter' || e.key === ' ') {
                        onClick && onClick();
                    }
                }}
            >
                <div className="p-8 pr-32">
                    <h2 className="text-2xl font-bold mb-4 leading-tight">{title}</h2>
                    <p className="text-base leading-relaxed">{description}</p>
                </div>
            </div>

            <div className="sw-angled-image absolute top-0 right-4 w-64 h-96 z-20">
                <div className="w-full h-full relative">
                    {imageUrl ? (
                        <>
                            <img 
                                src={imageUrl} 
                                alt={imageAlt || "Card image"} 
                                className="absolute inset-0 w-full h-full object-cover"
                                style={{'clipPath': 'polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%)'}}
                            />
                            <div className="absolute inset-0 bg-red-500 opacity-60 mix-blend-multiply" 
                                 style={{'clipPath': 'polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%)'}}></div>
                        </>
                    ) : (
                        <div className="absolute inset-0 flex items-center justify-center text-white bg-gradient-to-br from-red-400 to-red-600" 
                             style={{'clipPath': 'polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%)'}}>
                            <div className="text-center p-6">
                                <div className="text-base font-bold">SPORT WALES</div>
                                <div className="text-sm opacity-90">Image Placeholder</div>
                            </div>
                        </div>
                    )}
                    
                    <div className="absolute top-1/2 left-6 transform -translate-y-1/2 z-30 pointer-events-none">
                        <div className="bg-white bg-opacity-20 rounded-full p-3 backdrop-blur-sm">
                            <svg className="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M8.59 16.59 L13.17 12 L8.59 7.41 L10 6 L16 12 L10 18 L8.59 16.59Z" />
                            </svg>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    );
}`,
    tailwind: `<!-- Interactive Card with Angled Image Container (Updated Structure) -->
<div class="relative w-full min-h-96 p-10 bg-gray-50" style="font-family: 'Montserrat', sans-serif;">
    <!-- Card Content (positioned on left side, behind image, clickable) -->
    <div class="sw-card-interactive relative w-80 bg-teal-400 hover:bg-teal-500 text-white shadow-lg cursor-pointer transition-colors duration-300 z-5" 
         style="height: 280px; margin-top: 2rem;" onclick="handleCardClick()">
        <div class="p-8 pr-32">
            <h2 class="text-2xl font-bold mb-4 leading-tight">Grants & funding</h2>
            <p class="text-base leading-relaxed">
                We distribute funding to community clubs, volunteers, and athletes. Our grants team can support you with information.
            </p>
        </div>
    </div>

    <!-- Angled Image Container (in front, slightly wider) -->
    <div class="sw-angled-image absolute top-0 right-4 w-64 h-96 z-20">
        <div class="w-full h-full relative">
            <!-- Replace with your Sport Wales image -->
            <img src="your-sport-wales-image.jpg" 
                 alt="Sport Wales - Your content description" 
                 class="absolute inset-0 w-full h-full object-cover"
                 style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);" />
            
            <!-- Red overlay filter for Sport Wales branding -->
            <div class="absolute inset-0 bg-red-500 opacity-60 mix-blend-multiply"
                 style="clip-path: polygon(30% 0%, 100% 0%, 70% 100%, 0% 100%);"></div>
            
            <!-- Interactive Arrow (positioned on left side of angled area) -->
            <div class="absolute top-1/2 left-6 transform -translate-y-1/2 z-30">
                <div class="bg-white bg-opacity-20 rounded-full p-3 backdrop-blur-sm hover:bg-opacity-30 transition-all duration-300">
                    <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M8.59 16.59 L13.17 12 L8.59 7.41 L10 6 L16 12 L10 18 L8.59 16.59Z" />
                    </svg>
                </div>
            </div>
        </div>
    </div>
</div>

`
}
            
        }
    };

    return componentData;
}

// Primary card
ReactDOM.render(
    <CodeTabsComponent codeExamples={ComponentInstructions().codeExamples.primary} tabId="primary" />,
    document.getElementById('primary-code-tabs')
);

// Interactive card
ReactDOM.render(
    <CodeTabsComponent codeExamples={ComponentInstructions().codeExamples.interactive} tabId="interactive" />,
    document.getElementById('interactive-code-tabs')
);
{% endraw %}
</script>

<!-- Responsive styles for mobile - using Tailwind responsive prefixes -->
<style>
/* Angled image container styling */
.sw-image-container {
    /* Creates the angled cut on left side matching the uploaded image design */
    clip-path: polygon(25% 0%, 100% 0%, 100% 100%, 0% 100%);
}

/* Only keeping essential React container styling */
#primary-code-tabs {
    height: 100%;
    display: flex;
    flex-direction: column;
}

#interactive-code-tabs {
    height: 100%;
    display: flex;
    flex-direction: column;
}

/* Mobile responsive adjustments */
@media screen and (max-width: 576px) {
    .sw-card-a, .sw-card-b {
        max-width: 100%;
        margin-inline: auto;
    }
    
    .sw-card-a .p-8, .sw-card-b .p-8 {
        padding-right: 2rem;
    }
    
    .sw-card-a .absolute.right-0, .sw-card-b .absolute.right-0 {
        width: 6rem;
    }
    
    /* Adjust clip-path for mobile */
    .sw-image-container {
        clip-path: polygon(30% 0%, 100% 0%, 100% 100%, 0% 100%);
    }
}
</style>
{% endblock %}